package chapter_03;


/**
 * 8. The following code fragment will not compile. Why?
 * if {b+c > !a}
 *  c = b / a;
 * else
 *  c = b % 2;
 *
 *
 * @author Sharaf Qeshta
 *  */
public class Exercise_03_08
{
    // you cannot use curly brackets {} instead of parenthesis ()  ( if {b+c > !a} )
    // you cannot negate integers  ( !a )
}
