package chapter_03;


/**
 * 9. Consider the boolean expression ((10 > 7) && (z >= 50)). Why is this
 * expression probably not what the programmer intended?
 *
 * @author Sharaf Qeshta
 * */
public class Exercise_03_09
{
    // (10 > 7) it`s always true typing this condition is redundant
    // z >= 50 this will do the job
}
