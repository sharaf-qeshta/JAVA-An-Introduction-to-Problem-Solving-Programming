package chapter_10;

/**
 * 5. Modify the class LapTimer, as described in Exercise 13 of Chapter 9, as
 * follows:
 * • Add an attribute for a file stream to which we can write the times
 * • Add a constructor
 * LapTimer(n, person, fileName)
 * for a race having n laps. The name of the person and the file to record the
 * times are passed to the constructor as strings. The file should be opened
 * and the name of the person should be written to the file. If the file cannot
 * be opened, throw an exception.
 *
 *
 * @author Sharaf Qeshta
 * */
public class Exercise_10_05
{
    public static void main(String[] args)
    {
        // see LapTimer.java
    }
}
